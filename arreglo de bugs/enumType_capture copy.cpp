#include <iostream>
using namespace std;
enum prro
{
    MASTER = 1,
    CANIJO,
    CHALAN
};

struct cato
{
    prro *cerdo;
};

prro *marro(prro *);

int main()
{
    cato *chinga;
    prro *morro;
    prro *gato;
    gato = marro(morro);
    scanf("%i",&chinga->cerdo);
    printf("chinga->cerdo %i",chinga->cerdo);
    return 0;
}

prro *marro(prro *jabali)
{
    cout << "mierda" << endl;
    while (jabali != 0)
    {
        cout << "carajo" << endl;
        scanf("%i", &jabali);
        cout << "chingao" << endl;
        printf("> %i\n", jabali);
    }
    cout << "sali?" << endl;
    return jabali;
}